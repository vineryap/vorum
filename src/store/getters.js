export default {
  // totalThreads: (state) => (state.stats.postsCount = state.threads.items.length),
  // totalPosts: (state) => (state.stats.threadsCount = state.posts.items.length),
  // totalUsers: (state) => (state.stats.usersCount = state.users.items.length)
}
